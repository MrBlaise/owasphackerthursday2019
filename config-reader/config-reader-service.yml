apiVersion: v1
kind: Service
metadata:
  name: config-reader
  labels:
    app: config-reader
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: config-reader
